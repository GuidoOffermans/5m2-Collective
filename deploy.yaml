import:
  - recipe/laravel.php

config:
  repository: 'https://github.com/GuidoOffermans/5m2-Collective.git'

hosts:
  194.13.82.194:
    remote_user: atheros
    deploy_path: '~/apps/5m2-Collective'

tasks:
  build:
    - cd: '{{release_path}}'
    - run: 'npm run build'

after:
  deploy:failed: deploy:unlock
